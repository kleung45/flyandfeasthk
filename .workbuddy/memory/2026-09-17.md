# 2026-09-17 工作紀錄

## 社群留言自動回覆（14:32 第十三次執行）— 未發送

- dry-run 掃描 35 則（FB 21 + Threads 14），準備回覆 1 則（threads @cyk_dr.eat「優惠」）。
- **發現並行執行衝突**：14:37:56 `engage_state.json` / `engage_log.jsonl` 被另一工作階段寫入，
  該留言已被回覆（replyId 17905527513330782）。本輪只跑 dry-run，故絕非本輪所發。
- 因兩個工作階段同時寫同一狀態檔有 race condition，**未執行 --live**，零對外發送。
- 掃描池由 42 縮至 35：**當時歸因為「7 日窗口前移」，此說法有誤，已於 15:39 更正**（見下）。

## 修正既有紀錄錯誤

- 累計回覆實為 **10 則**（過往紀錄一直寫 9 則）。首筆 `17905949142483159`（@vincyv2024）
  一直存在於 `replied`，只是歷次結算漏計。已核 10 筆全有 replyId 且與 log 逐筆對應。

## 待用戶決定

1. 此自動化目前約每小時一次，連續多輪零成果 → 建議降頻。
2. 排程時段需錯開，避免與其他任務同時寫入同一狀態檔。

## 社群留言自動回覆（15:39 第十四次執行）— 未發送，修正掃描窗口

- dry-run：準備回覆 **0 則** → 依規則未執行 --live，零對外發送。
- **更正 14:32 那輪嘅歸因**：帖文跌出掃描池唔係因為「7 日窗口」，而係因為
  `--max-posts` 預設 20。經 Threads API 核實，帖文 `18333324127278192`（9/13 發）
  在 media 列表排 **index 21**，剛好被切走；其 3 則回覆本身仍在 7 日內。
  換言之，發文頻率一高，20 則窗口就會滑掉 4 日前的帖文。
- **修正**：`pipeline/engage.py` 的 `--max-posts` 預設由 20 改為 **30**
  （Threads `/threads` 端點本身只回約 30 則，30 等於全覆蓋）。
  風險：每輪 API 讀取次數增加約 10 次，全部為唯讀 GET，無對外動作。
- 修正後重跑 dry-run：掃描 52 則（FB 30 + Threads 22），仍為 0 則待回覆，
  略過＝自家留言×42 + 已回覆×10，10 筆歷史回覆全數回到池內，證實只影響舊紀錄、無漏客。
- 教訓：**「掃描池變動」要先分辨是日期窗口還是筆數窗口**，兩者成因不同、修法不同。
