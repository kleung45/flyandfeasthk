# 2026-09-16

## 站點 SEO／變現基建（21:47–22:00）
- 用戶確認目標：優惠拉流量入站 → Google AdSense 變現，要求建議同動手。
- 已完成並部署（commit 7e51cd28b → kleung45/flyandfeasthk@main）：
  - `build_site.py` 新增 GA4 注入：讀 `store.json` `meta.ga4MeasurementId`（空則只留佔位註解），ID 格式校驗 `G-` 開頭；index.html 加 `SEO:GA4` marker 區。
  - 聯盟/商戶外流連結由 `rel="noopener nofollow"` 改為 `rel="noopener sponsored"`（build_site.py `render_card` + `assets/app.js` 同步），20 張卡已重渲染上線。
  - `store.json` meta 加 `ga4MeasurementId: ""` 欄位。
  - deploy 用 `pipeline/deploy.sh`（注意：工作區 bash 會誤彈 WSL，要用 PortableGit 全路徑呼叫）。
- 待用戶提供 GA4 Measurement ID（G-XXXX）→ 填入 store.json → rebuild + deploy 即生效。
- 22:00 用戶改用 GTM：提供 GTM-KQVGKZTS。已擴充 build_site.py 支援 `meta.gtmContainerId`
  （head script + body noscript 雙注入，`SEO:GTM-HEAD/BODY` marker），store.json 已填值，
  部署上線 commit 3b233117b。GA4 tag 之後由用戶喺 GTM 後台設定，`ga4MeasurementId` 留空。
- 22:27 用戶開好 GA4 資源（G-83BHD7MNDL，帳戶 YookDesign 下新資源 Fly & Feast HK；
  注意帳戶入面另有一個舊資源「Yook」屬其他站，唔好混用）。GA4 gtag.js 直接注入站內
  （meta.ga4MeasurementId），部署 commit 828c31433。**警告用戶：GTM-KQVGKZTS 容器內
  唔好再加 GA4／Google tag，否則雙重計數**；GTM 容器留空載入無害。
- 已提醒用戶：AdSense 後台確認 Auto ads 開啟；GSC 提交 sitemap；變現組合建議 = AdSense + Klook/Trip.com affiliate + WhatsApp Channel 引流。
- 本日稍早：engage.py 自動化第十輪 dry-run，0 新留言待回覆（連續九輪零外部新留言）。
