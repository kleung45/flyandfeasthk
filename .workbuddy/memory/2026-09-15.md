# 2026-09-15 工作日誌

## 社群留言自動回覆（首次執行，15:39）
- 對 Fly & Feast HK 的 FB 專頁與 Threads 執行 `pipeline/engage.py` dry-run。
- 結果：掃描 41 則留言／回覆（自家 32、已回覆過 9），**準備回覆 0 則**，因此依規則未執行 `--live`。
- 憑證與權限正常，兩平台讀取無錯誤。
- 同日 14:32–14:36 另有一次 live 執行（非本自動化），回覆 9 則 Threads 留言。

## 程式修正
- `pipeline/engage.py`：`build_candidates()` 有死碼問題 —— 回傳 `(scanned, scanned)`，
  而 `candidates` 清單建了卻從未填充、呼叫端也用不到。改名為 `collect_comments()`，
  只回傳 `scanned`，`main()` 同步調整。重跑 dry-run 輸出一致，無回歸。
- 抽取到的經驗：此類「關鍵詞命中 → 派連結」的腳本，收集與過濾應分離，
  收集函式不該回傳兩個相同的清單。

## 慣例補充
- 這類情報／社群自動化任務的「準備回覆 0 則」是**健康狀態**，不是失敗；
  判斷健康與否要看略過原因分佈，而非待回覆數量。
